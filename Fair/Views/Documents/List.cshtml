@model List<Document>

@{
  ViewData["Title"] = "Documents";
  var search = ViewBag.Search;
}

<nav>
  <ol class="breadcrumb">
    <li class="breadcrumb-item"><a asp-controller="Searches" asp-action="List">Searches</a></li>
    <li class="breadcrumb-item"><a asp-controller="Searches" asp-action="View" asp-route-id="@search.SearchId">@search.Name</a></li>
    <li class="breadcrumb-item active">Documents</li>
  </ol>
</nav>

<ul class="nav nav-tabs">
  <li class="nav-item">
    <a class="nav-link" asp-controller="Searches" asp-action="View" asp-route-id="@search.SearchId">Summary</a>
  </li>
  <li class="nav-item">
    <a class="nav-link active" href="#">Documents (@Model.Count)</a>
  </li>
  <li class="nav-item">
    <a class="nav-link" href="../Applications/List">Applications (0)</a>
  </li>
</ul>

<p class="text-right mt-2"><a href="Add" class="btn btn-primary">New Document</a></p>

<table class="table table-hover">
  <thead>
    <tr>
      <th>Name</th>
      <th>Uploaded By</th>
      <th>Date</th>
    </tr>
  </thead>
  <tbody>
    @foreach (var document in Model)
    {
      <tr>
        <td><a href="View/@document.DocumentId">@document.Name</a></td>
        <td>@document.LatestRevision.Author.Name</td>
        <td>@document.LatestRevision.Timestamp.ToString("d")</td>
      </tr>
    }
  </tbody>
</table>
